<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %> <!-- renderiza el partial _shim -->
  </head>
  <body>
    <%= render 'layouts/header' %> <!--renderiza el header -->

    <div class="container">

      <!--muestra los mensajes de devise ya sea require sign_in o sign_up o errores en user password ya que inicialmente
      esta el filtro authenticate_user! en application_controller -->

        <% flash.each do |message_type, message| %>
          <%= content_tag(:div, message, class: "alert alert-#{message_type}") %>
        <% end %>


        <%= yield %>  <!--renderiza las vistas (inicialmente la vista posts/index.html.erb) -->


        <%= render 'layouts/footer' %> <!--renderiza el footer -->

        <%= debug(params) if Rails.env.development? %>

    </div>
  </body>
</html>
